/**
 * Use case service. Coordinates the use case execution.
 * It can run validations, check some high-level policies, and persist the model.
 */
define([], function () {
  'use strict';

  return {
    save : function (model, presenter) {
      // ...
    }
  };
});