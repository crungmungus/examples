define(['app'], function (app) {
  'use strict';

  // Liaise with router to know current place?
  // I'm thinking of highlighting options on the menu and throwing out callbacks for sub-menu events etc.
  // These sub-menu events can be handled by the presenter of each module or .. the view.
  var Director = {

  };

  return Director;
});